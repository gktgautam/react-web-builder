{
  "name": "react-page-builder-workspace",
  "private": true,
  "packageManager": "pnpm@9.6.0",
  "workspaces": [
    "web",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -w --parallel dev",
    "build": "pnpm -w --parallel build",
    "typecheck": "pnpm -w --parallel typecheck"
  },
  "overrides": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
